import { __experimentalGrid as Grid } from "@wordpress/components";
import Box from "../Box";

const TabThree = () => {
  return (
    <Grid
      columns={1}
    >
      <Box/>
    </Grid>
  );
};

export default TabThree;